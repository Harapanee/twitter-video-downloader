requests>=2.28.0
# Optional: required only for Cloudflare-protected hosts (e.g. playmogo.com /
# DoodStream embeds). If unavailable, those hosts won't work but everything
# else still does.
curl_cffi>=0.5
